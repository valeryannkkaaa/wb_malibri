# STATE — wb_malibri (модуль рекламы WB, клиент Малибри)

Обновлено 25.07.2026. Файл перезаписывается, а не дописывается. Подробности — в памяти проекта.

## Что сейчас

Интеграционная ветка **`feature/docker-advert-germany`** = `origin` = germany. `main` отстаёт
намеренно: мержим после сдачи. Стенд https://wb.zhukovlab.ru/advert — **тестовая** среда.
Оптимизатор в `suggest-only`, `allow_wb_writes=false` — только советует.

Сбор данных из ВБ (#15–#19) работает по cron каждые 4 часа. Покрытие ротацией на 25.07: воронка
9/10, поисковый отчёт 7/10, позиции и конкуренты ежедневно. Прогон 25.07: `lower_bid` 18,
`raise_bid` 1, `keep` 2. Зонд свежести (#20) крутится на germany каждые 10 минут.

**Внимание: все 10 пилотных кампаний на паузе** (`status 11`), баланс рекламы 0 ₽ — проверено
25.07 по API. У перчаток 24.07 было 174 показа, за весь 25.07 — 2. Владелец передаёт информацию
26.07. Пока кампании стоят, решения применять некуда, а зонд пишет плоскую линию: это отсутствие
событий, а не свойство API. Статусу в `pilot_skus.csv` не верить — он не обновляется.

## Блокеры и долги

1. **Ротировать WB API-токен** — лежал в публичном репо (`wb_advert_probe/Untitled`) с начального
   коммита. Файл убран (`cd08dd9`), история не переписана → нужна ротация в ЛК, затем новый токен
   в `/opt/wb-advert/.env` и рестарт `wb-advert`.
2. **issue #10** — портальная шапка на germany незакоммичена (`app.py`, `advert.css`, `_nav.html`,
   `_portal_nav.html`). Любой `git pull` мимо нас её снесёт, бэкап в `/root/backups/`.
3. **Расход занижен**: `sync/mappers.py:37` достраивает `spend` как `cpc × clicks` и теряет деньги
   по ключам без кликов, хотя ВБ отдаёт настоящий `spend`. На решения не влияет, но врут выгрузка
   менеджеру, снапшоты и ДРР. Чинить после того, как зонд наберёт статистику.
4. **Пауза не видна в UI**: дашборд и журнал решений не помечают остановленные кампании.
5. **Анализатор зонда не написан**: считать по `data/probe/flat/*.csv`, когда меняются
   `fs_views`/`fs_clicks`/`fs_sum` между опросами — это и есть шаг обновления ВБ.
6. Перед включением записи ставок — ограничение «не чаще раза в сутки на ключ» (ВБ переобучается
   24–72 ч).
7. 87% ключей на приорной оценке CR: кормить оптимизатор конверсиями из воронки за 365 дней.
8. Неатомарная запись в `keywords_store`/`search_report_store`/`funnel_store`.
9. Два источника позиций не сведены: парсер выдачи vs `median_position`. Владелец отложил.

## Запуск и деплой

```bash
# локально
cd wb_advert && cp .env.example .env   # вписать WB_API_TOKEN
python -m scripts.sync_once && python -m scripts.run_optimizer
uvicorn wb_advert.app:app --reload

# germany (ОСТОРОЖНО: пункт 2 — 4 файла шапки незакоммичены)
ssh germany && cd /opt/wb_malibri && git pull
docker compose -f deploy/wb-advert.compose.yml up -d --build
docker exec wb-advert bash -lc "cd /app/wb_advert && python -m scripts.run_optimizer"
```

Последняя строка обязательна: дашборд показывает **сохранённые** решения, иначе видны старые.
Боевой cron `/etc/cron.d/wb-advert` — каждые 4 часа под `flock /tmp/wb-advert-cycle.lock`.
Зонд `/etc/cron.d/wb-advert-probe` — каждые 10 минут, данные в `data/probe/`, остановить `rm`.

## Правило работы

Задача → допрос владельца → issue → исполнитель в orca-worktree → **проверка мутациями
продакшн-кода** → мерж в интеграционную → деплой → **живая проверка своими глазами** → ревью
комментарием в issue. Отчётам исполнителя не верить.
